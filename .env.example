# Production site URL (no trailing slash) — used for canonical, OG, JSON-LD
VITE_SITE_URL=https://ardhnarishwar.com

# Google Form embed (optional) — from Form → Send → embed:
# https://docs.google.com/forms/d/e/YOUR_FORM_ID/viewform?embedded=true
VITE_GOOGLE_FORM_EMBED_URL=
